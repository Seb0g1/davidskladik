registerOperationsRoutes(app, {
  requireAdmin,
  cleanLimit,
  cleanText,
  crypto,
  readOperationJobs,
  upsertOperationJob,
  operationJobPublic,
  operationTitle,
  startOperationJob,
  activeOperationJobs,
});
