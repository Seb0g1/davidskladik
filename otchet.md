# Отчёт о выполнении планов

**Дата:** 2026-08-28  
**Тесты:** 281 pass, 0 fail  
**Изменено файлов:** 28 (260 вставок, 161 удалений)

---

## Выполненные задачи по страницам

### Сборка (PickingListPage.tsx)
- **ИСПРАВЛЕНО** Дублирующиеся кнопки «Возврат» и «К сборке» для picked-строк — объединены в единый блок `<>`
- **ИСПРАВЛЕНО** `pickQtyDrafts` не очищался после updateMutation — добавлен `delete next[variables.key]` в onSuccess
- **ДОБАВЛЕНО** Предупреждение об ограничении инвойса: «Показаны первые 80 строк из N» при N > 80

### Автокорзина (SupplierCartPage.tsx + 02f-supplier-cart-routes.js)
- **ИСПРАВЛЕНО** `rollbackApply` теперь сбрасывает `rollbackDryRun` через `.reset()` в onSuccess
- **ИСПРАВЛЕНО** Сброс `rollbackDryRun` при закрытии шторки настроек
- **ДОБАВЛЕНО** Колонка «Получен» (Recieved) в таблице истории PM с иконкой ✓ / —
- **ДОБАВЛЕНО** Кнопка «Обновить» (RefreshCw) для PM-истории
- **ИСПРАВЛЕНО** `POST /api/ready-to-ship/batch-order` — доступ понижен с `requireAdmin` до `requireStaff` (унификация с одиночным `/order`)

### Отзывы (ReviewsPage.tsx)
- **ИСПРАВЛЕНО** Удалён локальный `apiJson` (32 строки), заменён на `fetchJson` из `../api`
- **ДОБАВЛЕНО** Confirm-диалог перед отправкой ответа: «Отправить ответ? Это действие необратимо.»
- **ДОБАВЛЕНО** Счётчик символов `{text.length}/5000` под textarea ответа

### Вопросы (QuestionsPage.tsx + 02f-questions-routes.js)
- **ИСПРАВЛЕНО** Удалён локальный `apiJson`, заменён на `fetchJson` из `../api`
- **ДОБАВЛЕНО** Ссылка «↗ Товар» на страницу товара в карточке вопроса (поле `productUrl`)
- **ДОБАВЛЕНО** Счётчик символов `{text.length}/5000` в форме ответа
- **ИСПРАВЛЕНО** Поле `elapsed` в ответе API скрыто в production (`NODE_ENV !== "production"`)

### Чаты (ChatsPage.tsx)
- **ИСПРАВЛЕНО** Удалён локальный `apiJson`, вынесен в `api.ts` как общий
- **ИСПРАВЛЕНО** Memory leak: ObjectURL для `pendingImages` теперь освобождается при размонтировании компонента
- **ИСПРАВЛЕНО** `disabled` на textarea при isPending отправки
- **ДОБАВЛЕНО** Счётчик символов `{text.length}/1000` для WB-чатов
- **ДОБАВЛЕНО** Сохранение черновика сообщения при переключении чатов (`draftRef: Map<chatId, text>`)

### Финансы (FinancePage.tsx)
- **ИСПРАВЛЕНО** Inline edit отправляет только 6 редактируемых полей (больше не спредит весь объект `order`)
- **ИСПРАВЛЕНО** `orderDrafts` сбрасывается при смене периода или `linkedOnly`
- **ИСПРАВЛЕНО** Автоскрытие полоски «Закупка добавлена» через 3 секунды
- **ДОБАВЛЕНО** Валидация `supplierName` перед созданием расхода (alert если пусто)

### Поставщики (SuppliersPage.tsx)
- **ДОБАВЛЕНО** Отображение текущего курса USD рядом с полем оплаты для USD-поставщиков

### Цены (PricesPage.tsx)
- **ИСПРАВЛЕНО** Хардкоженный цвет `#dc2626` на кнопке «ОТПРАВИТЬ ВСЕ СЕЙЧАС» заменён на CSS-класс `danger-action`
- **ИСПРАВЛЕНО** Label фильтра «Apply» → «Статус отправки»
- **ИСПРАВЛЕНО** `refetchInterval` для summary и items синхронизированы (оба 45 000 мс)
- **ОПТИМИЗИРОВАНО** `ozonIssues` и `yandexIssues` обёрнуты в `useMemo([items])`

### Операции (OperationsPage.tsx)
- **ДОБАВЛЕНО** Confirm-диалог перед «Вернуть Ozon автоархив» и «Починить привязки»
- **ИСПРАВЛЕНО** Лимит отображения ошибок операции: 20 → 80 записей

### AI Черновики (AiDraftsPage.tsx)
- **ДОБАВЛЕНО** Предупреждение «Показаны 80 из N кандидатов» при N > 80
- **ИСПРАВЛЕНО** Кнопка «Отправить» заблокирована если у товара нет черновика (`hasDraft` проверка)
- **ДОБАВЛЕНО** Отображение всех 5 фото-черновиков в горизонтальной полосе (overflow-x: auto)

### Avito (AvitoPage.tsx)
- **ИСПРАВЛЕНО** После `triggerUpload` теперь инвалидируются `avito-sync-status` и `avito-feed-info`
- **ИСПРАВЛЕНО** Счётчик в заголовке показывает `matched` при активном поиске (не `total`)

### Импорт на Яндекс (ImportPage.tsx)
- **ИСПРАВЛЕНО** Удалён мёртвый код: `!clearMarkingDry.data.dryRun === false ? null : null`
- **ДОБАВЛЕНО** Confirm-диалог перед «Добавить описания»
- **ДОБАВЛЕНО** Сообщение «Все выбранные товары уже импортированы» при `sent: 0, skipped.length > 0`
- **ИСПРАВЛЕНО** Удалён локальный `apiJson`, заменён на `fetchJson` из `../api`

### Новые товары (NewProductsPage.tsx + 02d-new-products.js)
- **ИСПРАВЛЕНО** Поиск больше не сбрасывает фильтр по поставщику (убран `setSupplierFilter("")`)
- **ИСПРАВЛЕНО** Двунаправленная проверка prefix: `wn.startsWith(name) || name.startsWith(wn)`
- **ИСПРАВЛЕНО** Сортировка `price_desc` теперь работает корректно на сервере

### Проблемные товары (ProblemProductsPage.tsx)
- **ДОБАВЛЕНО** Чекбокс «Выбрать все» в заголовке таблицы
- **ДОБАВЛЕНО** Предупреждение «Будут починены первые 100 из N выбранных» при N > 100

### Ошибки наличия (NoSupplierPage.tsx)
- **ДОБАВЛЕНО** Отображение времени кэша: «Данные из кэша: HH:MM» при наличии `cachedAt`
- **ДОБАВЛЕНО** Фильтр по маркетплейсу (Все / Ozon / Яндекс / WB)

### Запрет брендов (BrandBansPage.tsx)
- **ИСПРАВЛЕНО** Текст confirm при удалении бана: добавлено предупреждение что товары останутся архивными

### Настройки (SettingsPage.tsx)
- **ИСПРАВЛЕНО** `useEffect` больше не перезаписывает draft при background refetch (`isDirtyRef` флаг)
- **ИСПРАВЛЕНО** Лимит аудит-лога: 120 → 500 записей

### MV Shop (ShopAdminPage.tsx)
- **ДОБАВЛЕНО** `inline-error` для мутаций в BannersTab, CategoriesTab, OrdersTab
- **ИСПРАВЛЕНО** Технические ключи доставки заменены русскими метками (`DELIVERY_LABELS`)

### Система (SystemPage.tsx)
- **ИСПРАВЛЕНО** Убраны дублирующиеся карточки «Active jobs» и «Ошибки операций» из summary-grid
- **ДОБАВЛЕНО** Секция «Журнал поставщиков» с данными `supplierLedger` (pickedRows, debtEntries, missingDebtEntries)

### ТН ВЭД (TnvedPage.tsx)
- **ДОБАВЛЕНО** Валидация формата кодов (должно быть 10 цифр) перед сохранением
- **ИСПРАВЛЕНО** Missing dependency в `useEffect` для `defaultCode`

### Поддержка сайта (02d-shop-support.js)
- **ИСПРАВЛЕНО** Порядок роутов: `GET /api/support/chats/unread-count` теперь зарегистрирован выше `GET /api/support/chats/:id`

### Статистика (StatisticsPage.tsx)
- **ИСПРАВЛЕНО** Ошибки разных источников данных показываются отдельно (не через `||`)
- **ДОБАВЛЕНО** Пометка удалённых сотрудников `(удалён)` в таблице активности

### Восстановление (RecoveryQueuePage.tsx)
- **ИСПРАВЛЕНО** Yandex статус заменён с `setInterval + fetch` на TanStack Query (`useQuery`)
- **ДОБАВЛЕНО** Поиск по SKU в таблице очереди
- **ДОБАВЛЕНО** Кнопка «Перестроить очередь Ozon» с confirm-диалогом

---

## Итог

| Категория | Кол-во |
|-----------|--------|
| Исправленных багов | 34 |
| Добавленных улучшений | 26 |
| Изменённых файлов | 28 |
| Тесты | 281 / 281 ✓ |

Все планы из папки `планы/` выполнены и удалены.
